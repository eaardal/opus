{
  "name": "integration-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ci": "firebase emulators:exec --only firestore 'vitest run'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.1",
    "firebase": "^10.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.1"
  }
}
